# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

Mailshop 面向多个独立跨境电商商家及其运营团队。主要用户是跨境电商选品与商品运营人员，他们在浏览供应链商品、整理商品资料、寻找 1688 货源并维护 Shopify 店铺时使用产品。管理员负责账号、系统集成和审计等平台级工作。

## Product Purpose

Mailshop 将商品采集、图片搜索找货、人工确认和 Shopify 商品导入连接成一个可追踪的工作闭环。用户可以从网页、浏览器扩展或 CSV/JSON 文件创建采集任务，保存商品图片和搜索轮次，确认 1688 候选货源后导入 Shopify 草稿商品。产品成功意味着减少重复搬运和等待，节省商品上架时间，并提高找货准确率。

## Positioning

Mailshop 的核心机制是把“采集任务”作为跨平台工作单元：每个商品的来源资料、图片、1688 搜图轮次、候选货源、人工匹配判断和 Shopify 导入状态都围绕同一任务持续保存。它不是单纯的商品后台、图片搜索器或批量导入脚本，而是连接这些步骤的运营工作台。

## Operating Context

- 用户在商品来源网站或浏览器扩展中收集商品标题、描述、SKU、来源 URL 和一张或多张图片。
- 用户可以通过 CSV/JSON 批量导入采集任务，也可以在任务中逐张图片发起 1688 图片搜索。
- 搜索结果会保留搜索轮次、分页、积分消耗和候选货源；用户确认匹配状态后再导入 Shopify。
- Shopify 导入前需要先配置目标店铺并通过连接测试；导入结果保存为 Shopify 草稿商品。
- 管理员在系统设置中维护 OneBound、Google 登录、AI 和 Shopify 集成，在账号管理与操作日志中处理平台运营。
- 服务部署在 Cloudflare Workers，D1 保存业务数据，R2 保存用户上传的商品图片；浏览器扩展通过 Mailshop Worker API 与后台协作。

## Capabilities and Constraints

- 支持管理员与普通用户角色；系统设置、账号管理和审计日志仅管理员可见。
- 支持 Google 账号登录、用户名/密码登录、会话 Cookie、登录限流和密码重置。
- 支持采集任务的创建、批量导入、搜索、分页、归档、恢复和回收站删除。
- 支持 Shopify 商品、SKU、选项、价格、库存、图片、视频和标签管理，以及单条或批量导入 Shopify 草稿商品。
- 支持一个 Shopify 商品关联多个 1688 Offer，并记录候选、已选和已拒绝状态、匹配分数、备注及 SKU 映射。
- 创建采集任务本身不扣除搜索积分；只有用户明确选择图片并执行 1688 搜图时才扣除积分。AI 请求和商品详情请求按现有积分规则计费。
- 同一用户的商品 URL 只能创建一个采集任务；批量导入最多接收 100 条记录，并逐行返回成功或失败结果。
- 对外部图片和 R2 媒体访问有协议、域名、内网地址、路径和登录校验等安全限制；敏感凭据必须加密存储，不能写入源代码或提交记录。
- 后端请求使用 Zod 校验，业务数据通过 D1 迁移管理；现有 API、数据表和浏览器扩展兼容性需要持续保留。
- Shopify 库存暂不自动写入；Shopify OAuth、Webhook 和双向同步属于后续方向，不能在未实现时对用户作出承诺。

## Brand Commitments

- 产品名称为 Mailshop。
- 产品面向跨境电商运营场景，现有界面和主要操作文案以中文为主，同时保留 Shopify、1688、SKU、CSV/JSON 等行业术语和英文专有名词。
- 用户需要清晰看到来源、搜索、匹配、审核和导入等工作状态；后续品牌设计不得削弱这些事实信息和操作反馈。
- 当前没有用户确认的固定字体、颜色、标语、Logo 规范或视觉参考；这些内容留待后续设计工作确定，不应被当作既有事实。

## Evidence on Hand

- [README.md](README.md) 与 [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md) 描述了产品定位、核心流程、API、数据模型、部署方式和安全约束。
- React/Vite 管理后台位于 `web/`，Worker API 与集成逻辑位于 `src/`，浏览器扩展位于 `image-search-extension/`。
- 仓库包含商品采集、Shopify 商品与店铺、积分、账号、设置、审计日志和批量导入等实际页面与组件。
- 仓库包含 Mailshop 管理后台和浏览器扩展的界面截图，可用于后续评估现有交互与视觉实现。
- 当前没有可公开使用的客户案例、用户评价或性能基准；后续工作不得虚构这些证据。

## Product Principles

1. 让商品从来源到 Shopify 的每一步都可追踪、可恢复、可解释。
2. 把人工判断放在关键匹配节点，同时用结构化数据减少重复录入。
3. 优先缩短高频运营流程中的等待和搬运时间。
4. 用来源证据、候选对比和明确状态帮助用户提高找货准确率。
5. 在多商家环境中隔离数据、权限和凭据，并保留关键操作审计。

## Accessibility & Inclusion

界面需要支持键盘操作、清晰的焦点状态、可读的状态与错误反馈，并在桌面和移动浏览器上保持可用。当前未指定额外的行业无障碍标准；后续设计应至少遵循 WCAG 2.2 AA 的可感知、可操作和可理解原则。
